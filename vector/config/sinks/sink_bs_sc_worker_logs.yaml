type: "http"
method: "post"
uri: "https://in.logs.betterstack.com/"
encoding:
  codec: "json"
auth:
  strategy: "bearer"
  token: "${VECTOR_SC_WORKER_LOGS_BEARER_TOKEN}"
inputs:
  - transform_sc_worker_logs